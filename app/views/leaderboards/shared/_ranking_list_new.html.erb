<!-- 새로운 랭킹 리스트 (메인페이지 스타일 제품 카드 사용) -->
<div class="rankboard-products-section">
  <% if @products && @products.any? %>
    <div class="results-info" style="margin-bottom: 1.5rem; color: #64748b; font-size: 0.875rem;">
      <span class="results-count"><%= @products.length %>개 제품</span>
    </div>

    <!-- 제품 카드 그리드 -->
    <div class="product-list" style="display: grid; gap: 1rem;">
      <% @products.each_with_index do |product, index| %>
        <div style="position: relative;">
          <!-- 순위 표시 (랭크보드 전용) -->
          <div style="position: absolute; top: 0.75rem; left: 0.75rem; z-index: 10; background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%); color: white; font-weight: 700; font-size: 1.5rem; width: 48px; height: 48px; border-radius: 8px; display: flex; align-items: center; justify-content: center; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);">
            #<%= index + 1 %>
          </div>

          <%= render 'shared/product_card', product: product, index: index %>
        </div>
      <% end %>
    </div>

  <% else %>
    <div class="empty-state" style="text-align: center; padding: 4rem 1rem; color: #94a3b8;">
      <i class="fas fa-inbox" style="font-size: 3rem; margin-bottom: 1rem; opacity: 0.5;"></i>
      <h3 style="font-size: 1.25rem; font-weight: 600; margin-bottom: 0.5rem; color: #64748b;"><%= @empty_message || "해당 기간에 등록된 제품이 없습니다" %></h3>
      <p style="color: #94a3b8;">다른 기간을 선택해보세요.</p>
    </div>
  <% end %>
</div>
